import { FontAwesomeIcon } from "@fortawesome/react-native-fontawesome";
import {
  faHeart,
  faCube,
  faSpa,
  faDiamond,
} from "@fortawesome/free-solid-svg-icons";
import { useEffect, useState } from "react";

export default function Suit() {
  // const [cardSuit, setCardSuit] = useState<string | null>(null);
  // const getCardSuit = () => {
  //   switch (suit) {
  //     case "spade":
  //       return <FontAwesomeIcon size={28} icon={faSpa} color={"black"} />;
  //     case "club":
  //       return <FontAwesomeIcon size={28} icon={faCube} color={"black"} />;
  //     case "heart":
  //       return <FontAwesomeIcon size={28} icon={faHeart} color={"red"} />;
  //     case "diamond":
  //       return <FontAwesomeIcon size={28} icon={faDiamond} color={"red"} />;
  //   }
  // };
  // return getCardSuit(suit)
}
